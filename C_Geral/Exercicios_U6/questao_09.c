#include <stdio.h>
#include <math.h>
double calcPolygon(n, b){
    double cotangent = 1 / tan(3.14159 / 4);
    return 1/4 * n * pow(b, 2) * cotangent;
}

int main(){
    int n, b;
    scanf("%d %d")
    return 0;
}